<!-- row_purchase_group.xml -->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- User Name -->
    <TextView
        android:id="@+id/textViewUserName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Total Cost -->
    <TextView
        android:id="@+id/textViewTotalCost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textViewUserName"
        android:layout_marginTop="8dp"
        android:text=""
        android:textSize="16sp" />

    <!-- Time of Purchase -->
    <TextView
        android:id="@+id/textViewTimeOfPurchase"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textViewTotalCost"
        android:layout_marginTop="4dp"
        android:text=""
        android:textSize="14sp"
        android:textColor="@android:color/darker_gray" />

    <!-- RecyclerView for Purchased Items -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewPurchasedItems"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/textViewTimeOfPurchase"
        android:layout_marginTop="8dp"
        android:scrollbars="vertical" />

</RelativeLayout>
